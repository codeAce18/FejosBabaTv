# Backend API (required — movies won't load without this)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Paystack (premium registration)
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_your_key_here

# Cloudinary widget (optional — admin can paste URLs manually)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dfm51g5gd
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=fejosbaba_unsigned
